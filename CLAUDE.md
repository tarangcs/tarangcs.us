# Working instructions

- After every change, publish/update the Artifact preview and give a clickable link, so the change can be viewed without a local server. Inline any local asset references as data URIs so the artifact renders standalone. Republish to the same artifact URL each time rather than creating a new one.
